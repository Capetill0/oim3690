# Learning Log Week 10 (03/31-04/02)

## What I learned this week

# Review Questions:

1. You wrote const total = price + tax and got "1005" instead of 15. What happened, and how do you fix it?
- The type of input needs to be changed. When it's like that it's using them as strings instead of numbers. 
    - parseFloat converts a string into a number

2. Your code has const count = 0 and later counter = counter + 1. It crashes. Why, and what's the one-word fix?
- neet to use let if they are changing. const is for what doesn't chage. 

3. Rewrite this using a template literal: "Hello," + name + "! You have " + count + "items." 
- 'Hello, ${nameInput}! Total:$${total.toFixed(2)}';

4. Why does 5 == "5" return true but 5 === "5" returns false?
- == just checks the value but === checks the type and the value

# Function: 
- a reusable block of code that performs a task:

By adding values and parameters, we make our code more flexible
- ex. greet(Isabel)

Functions with Parameters
- Parameters let functions work with different inputs each time you call them.

consolelog doesn't return the value to the outside, it just prints. By returning, it holds the variables to use later and prints it out it, it returns it. 

Use return when you think the output is going to be reused. 

# Return Values
- Use return to send a result back. Without it, the function returns undefined

# Arrow Functions
- Arrow functions are shorthand. AI-generated code uses them often, so learn to recognize them

Conditions are checked sequentially/ in order so the top line is checked first 

# Review Questions
1. You wrote function getGrade(score) but calling getGrade(85) returns undefined. What's most likely missing inside the function?
- there's no return statement, which doesnt return the result and doesn't store the info
- console.log just shows the nformation 

2. Your function checks if (score >= 60) first, then else if (score >= 90). What would a student with 95 get? Why?
- the student would just get a 'pass' instead of getting an 'A' because the conditions are in order/sequentially and if the forst condition is met, it stops there and doesn't check the other ones

3. What does this arrow function do? const f = x => x ** 2;
-  it will return as squared, so if x is 5, it will return 25

4. You wrote let username = prompt("Enter your name"); and the user clicked OK without typing anything. Does if (username) run?
- It's going to run, but it's just going to show Hello and it'll be blank unless the code had like an else if statement 

* Return will veto or immediately stop the function so even if there are two separate if statements, only the first one will run/print out? 

# AND (&&)
- Both conditions HAVE TO be true and this is useful for checking multiple requirements at once

# OR (||) 
- at least one condition must be true, and it's useful when any of the conditions will work

# NOT (!) 
- is just the opposite / not

# Form Validation
- This checks that email exists and password meets minimum length before allowing submission

# Arrays and Loops 
- An array stores multiple values in a single variable
- starts/counts from 0 instead of 1

# Modyfying Arrays
- push() adds to the end
- unshift() adds to the start

# Removing from Arrays
- pop() removes from the end
- shift () from the start 
- splice() to remove by index

# Array VS String 
- strings are immutable, so they cannot be changed
- arrays are mutable, so they can be changed 

